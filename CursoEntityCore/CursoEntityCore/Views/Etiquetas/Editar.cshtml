@model CursoEntityCore.Models.Etiqueta
@{
    ViewData["Title"] = "Editar Etiqueta";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />
<div class="container">
    <h3 class="text-center">Editar una Etiqueta</h3>
    <form method="post" asp-action="Editar">
        <!--Importante para editar-->
        <input type="hidden" asp-for="Etiqueta_Id" />

        <div class="row justify-content-center">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="col-12 col-md-8 mt-4">
                <div class="row mb-3">
                    <div class="col-4">
                        <label asp-for="Titulo"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Titulo" class="form-control" />
                        <span asp-validation-for="Titulo" class="text-danger"></span>
                    </div>
                </div>

                <div class="row">
                    <div class="col-4">
                        <label asp-for="Fecha"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Fecha" class="form-control" />
                        <span asp-validation-for="Fecha" class="text-danger"></span>
                    </div>
                </div>
                
            </div>
        </div>

        <div class="row justify-content-between align-items-center mt-4">
            <div class="col-4 text-start">
                <a asp-action="Index" class="btn btn-link">Volver a lista</a>
            </div>
            <div class="col-4 text-end">
                <button type="submit" class="btn btn-primary form-control">Editar</button>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
